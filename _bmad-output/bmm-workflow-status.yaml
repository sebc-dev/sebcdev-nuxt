# Workflow Status Template

# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-12-27"
project: "sebcdev-nuxt"
project_type: "blog"
selected_track: "method"
field_type: "greenfield"
workflow_path: "method-greenfield.yaml"
workflow_status:
  - phase: 0
    name: "Discovery (Optional)"
    workflows:
      - id: "product-brief"
        status: "_bmad-output/planning-artifacts/product-brief-sebcdev-nuxt.md"
        agent: "analyst"
        command: "/bmad:bmm:workflows:create-product-brief"
        completed: "2025-12-27"
        note: "Planification stratégique du produit - sélectionné par l'utilisateur"

  - phase: 1
    name: "Planning"
    workflows:
      - id: "prd"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-prd"
        output: "Product Requirements Document with FRs and NFRs"

      - id: "create-ux-design"
        status: "conditional"
        agent: "ux-designer"
        command: "/bmad:bmm:workflows:create-ux-design"
        note: "Déterminé après PRD - décision utilisateur/agent si nécessaire"

  - phase: 2
    name: "Solutioning"
    workflows:
      - id: "create-architecture"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:create-architecture"
        output: "System architecture document"
        note: "Conception système complète pour projet greenfield"

      - id: "create-epics-and-stories"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-epics-and-stories"
        note: "Requis: Décomposer le PRD en epics et stories implémentables avec contexte complet (PRD + UX + Architecture)"

      - id: "test-design"
        status: "recommended"
        agent: "tea"
        command: "/bmad:bmm:workflows:testarch-test-design"
        output: "System-level testability review"
        note: "Évaluation de testabilité avant gate check - détecte automatiquement le mode system-level"

      - id: "validate-architecture"
        status: "optional"
        agent: "architect"
        command: "/bmad:bmm:workflows:validate-architecture"
        note: "Vérification qualité pour complétude de l'architecture"

      - id: "implementation-readiness"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:check-implementation-readiness"
        note: "Valide PRD + Architecture + Epics + UX (optionnel)"

  - phase: 3
    name: "Implementation"
    workflows:
      - id: "sprint-planning"
        status: "required"
        agent: "sm"
        command: "/bmad:bmm:workflows:sprint-planning"
        note: "Crée le plan de sprint - travail ultérieur suivi là-bas"
